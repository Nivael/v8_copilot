# P8-0 数据门真实盘点

- plan：`P8DP-6753F0D179CF4A1B1C96`
- 截止：2026-09-03
- 形态容量 profile：`broad`（未读取未来结果）
- 公告 shortlist：8,362 条；已有正文 734，缺失 7,628
- 已有正文工作量：803 个分块调用、约 650 万字符；22 份公告需要多分块
- 正文缺口队列：7,628 条，按 `announcement_id` 去重并保留源 URL
- 累积特征：40,080/68,738 可完整计算
- 严格单日异常输入：37 条；满足相对 ST 涨幅与振幅条件的单日跳升形态 5 条
- qfq episode 路径：390/460
- 精确旧股东权益账：0（现有 pilot）
- owner 必须判断：0 项

## 结论

P8C 的持续型量价和 P8E 的 qfq 观察账可以直接运行；P8B 已能处理已有正文子集，并把其余
公告输出成可恢复队列。P8A 已登记退出制度版本、三类参考和唯一降级路径，但公司自身同一
claim 的成功/失败旧股东权益输入未闭合前，不发布统一分位或 `p*`。跨公司分层中位数只能
作为敏感性，不得自动变成公司自己的成功概率。

## 硬阻塞（只阻塞对应输出）

- 现有 P6B-2 pilot 的精确旧股东权益账为 0；P8A `p*` 与 P8E 精确权益账必须保持 unknown。
- 734 份公开公告正文尚未取得外部发送授权，因此 803 个结构化 LLM 调用保持未运行；API
  已在代码层固定 `store=false`。

## 安全默认

- 已有正文不因 `llm_route` 标签冒充 LLM 已运行；缺失正文继续保留在 denominator。
- `event_frontier` 只接受 `body_verified` 或 `deterministic_verified`，标题/临时节点只留在
  待补证探索层。
- 三类成交情景参考分账；公司自身同口径权益输入不闭合就 range/unknown。
- P8C 形态 profile 冻结为 broad，选择只使用覆盖与每日容量。
- 单日尖峰必须同时满足 P7 严格异常、当日相对 ST 回报和振幅条件，且不占持续型活动 lane。
- 筹码接口按各自 available-date 与缺失语义保存，不以零值替代。

## 三类参考库存

| 类型 | 候选/episode | 公司 | 正文/端点状态 | 精确旧股东口径 |
| --- | ---: | ---: | --- | ---: |
| 战略投资人 | 507 | 106 | 正文 78 | 0 |
| 失败退出 | 338 | 326 | 终端待核证 | 0 |
| 公开节点 | 9 | 9 | 市场端点可建 | 0 |

## 下一步

无需 owner 判断的物化已完成。后续仅剩两个时间/权限门：取得一次性正文外发授权后执行 734 份
公告的 803 个结构化调用；每日继续累积真实漏斗，分别走满 10 个交易日运营门与 60 个交易日
验证门，禁止倒造历史漏斗替代真实 shadow。
