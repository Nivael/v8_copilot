# v8 P6 PRD：管理人模式、重整前价值区间与方案价值重估

状态：proposed，等待 owner 评审后实施
日期：2026-07-23
负责人：Codex commander window
依赖：P4 market context；P5-C14 point-in-time market cap

## 1. 决策

P6 拆成三个独立产品能力，不合成一个“困境股评分”：

- **P6A 管理人历史模式**：材料化案件、管理人任职和关键节点，观察同一管理人在可比阶段的历史价格路径。
- **P6B 重整前独立价值区间**：只使用估值日已经存在的资产、业务、负债、上市平台期权和风险，回答“尚无正式方案时，现有股权价值大致在哪个范围”。
- **P6C 重整方案价值重估**：只在正式方案要素可得后，计算债务削减、新资金、资产变化和股本稀释带来的每股价值变化。

P6B 与 P6C 的估值对象、事实截止点和验证标签不同，禁止把 P6C 的方案信息回填进 P6B 历史估值。

## 2. 用户问题与产品边界

日常使用要支持：

1. 某只预重整/重整股票的管理人是谁，是否联合或更换，过去相似案件在各节点如何表现。
2. 市场急跌时，当前价格是公司价值恶化、ST/中小盘整体杀跌，还是相对价值错位。
3. 正式重整方案披露后，原股东的权益因偿债、注资、资产变化和稀释发生了什么。
4. 用户不需要具备专业估值能力，也不承担“批准模型数字准确”的责任。

仍保留 v8 红线：

- 不输出买入、卖出、持有、仓位、交易信号或排序权重。
- 可以输出价格相对价值区间的位置、下界跌破、情景敏感性和市场隐含条件。
- 浏览器只展示同一条 Codex 研究运行的证据与审计，不形成第二套研究主链。

## 3. 架构评审

### 3.1 当前架构

v8 已有：

- 只读 EvidencePack、AnswerCard、运行账本和来源校验；
- 2010-05 至 2026-07 的价格底盘；
- 270 条 `restructuring_path` episode、176 个 symbol，但均为 `case_note_only`，且旧样本存在选择偏差；
- ST 等权、中证2000、中证全指同窗市场语境；
- 与历史 membership 绑定的时点总股本和总市值；
- 公司公告入口和外部法院/管理人事实的 EvidencePack 网关。

当前缺少：

- 案件级管理人实体、别名、联合任职和任职区间；
- point-in-time 财务报表、资产处置、或有负债和重整方案事实层；
- 估值模型、估值区间校准、无未来数据回测和模型晋级门。

### 3.2 如果今天从零设计

会把事实、估值和验证拆开：

1. **Fact plane**：只保存带 `available_as_of`、来源和版本的原始事实。
2. **Model plane**：市场法、资产/成本法、收益法和方案桥分别运行，不能互相覆盖原始事实。
3. **Calibration plane**：冻结历史时点，走时序外样本；记录区间覆盖、宽度、误差和基线差。
4. **Answer plane**：只消费已晋级模型；模型不合格时展示事实组件和缺口，不伪造价值区间。

### 3.3 方案比较与决定

| 方案 | 优点 | 风险 | 决定 |
| --- | --- | --- | --- |
| 一个 LLM 读报告后给目标价 | 快 | 不可复现、无法校准、极易混入未来信息 | 拒绝 |
| 单一账面净资产/市净率 | 简单 | ST 资产质量和隐性负债差异太大 | 仅作基线 |
| 只用长期股价训练 | 市场真实 | 股价不是内在价值真值，且被增发、退市和方案改变污染 | 仅作结果校准 |
| 多方法区间 + 时序外样本 + fail closed | 可解释、能持续纠偏 | 建设量较大，初期会频繁返回缺口 | 采用 |

这是 hybrid 扩展：复用现有价格、episode、market context 和 EvidencePack；新增独立管理人事实层、估值事实层和校准层，不重写答案引擎。

## 4. P6A：管理人历史模式

### 4.1 数据对象

- `restructuring_case`：案号、公司主体、上市公司关系、法院、程序类型和状态。
- `administrator_organization`：标准名称、机构类型和统一身份。
- `administrator_alias`：简称、分所、历史名称和来源。
- `case_administrator_assignment`：管理人/临时管理人、主办/联合角色、起止日期、指定或更换依据。
- `restructuring_event`：预重整启动、正式受理、指定管理人、债权申报、投资人招募、投资协议、计划草案、表决、法院批准、实施和终结。

“律所”不是顶层字段。管理人可能是律所、会计师事务所、清算机构、个人或联合体。

### 4.2 来源顺序

1. 法院决定书和全国企业破产重整案件信息网；
2. 巨潮上市公司公告；
3. 管理人正式发布渠道；
4. 其他来源只能形成待补证候选。

### 4.3 事件研究

每个节点按交易日对齐，输出：

- `[-20,-1]`、`[0,+5]`、`[0,+20]`、`[0,+60]` 价格路径；
- 个股绝对收益及相对 ST、中证2000、中证全指的百分点差；
- 最大回撤、最大上行、成交活跃度和到下一节点时间；
- 同管理人、同阶段、同市场状态下的案例分布。

同一案件多个公告不能冒充多个独立样本。失败、退市和未完成案件必须保留；进行中案件按右删失处理。

### 4.4 输出门

- 所有任职必须有正式来源和有效日期。
- 联合管理人和更换管理人必须可表达。
- 小样本只列案例；达到冻结样本门后才显示分位分布。
- 禁止“该管理人导致上涨/成功”的因果表述。

## 5. P6B：重整前独立价值区间

### 5.1 估值对象

回答：

> 在估值日可见事实下，不使用尚未披露的重整方案或注入资产传闻，现有股东权益大致落在哪个区间？

### 5.2 三个独立模型

#### A. 调整后资产/成本法

从现金、应收、存货、固定资产、土地、投资和无形资产出发，按可核验质量折价，扣除：

- 有息债务；
- 预计负债；
- 对外担保和资金占用；
- 诉讼、税务、职工和破产费用；
- 无法核实的资产和权利瑕疵。

折价不是 LLM 随意生成。首版使用预注册的资产类别区间；有实际处置、评估或回收证据后再更新参数。

#### B. 持续经营/收益法

只对仍有可验证经营能力的公司启用。使用正常化收入、EBITDA、自由现金流和可比资本成本；连续停产、审计无法表示意见或现金流不可预测时直接禁用。

#### C. 上市平台市场期权

不使用一个固定“壳价”。使用估值日的历史 ST membership 和时点市值，从同阶段、同板块、相似退市风险和市场状态的案例估算市场分布。

为了避免与公司资产重复计算，不能直接把“同类 ST 总市值中位数”加到调整后净资产上。首版同时报告：

- 匹配案例的整体市值区间；
- 匹配案例扣除其可辨认净资产后的市场隐含剩余；
- 当前公司的资产法区间。

只有定义不重叠且验证通过时，才允许形成综合区间。

### 5.3 区间合成

- 两个高证据模型有重叠：展示重叠区和各模型完整区间。
- 模型无重叠：展示“无共识”，禁止机械平均。
- 只有一个合格模型：明确标记单方法估值。
- 所有模型不合格：只展示当前市值、历史相对市值和缺失事实。

输出保守、中性、乐观情景，但不得用未经披露的资产注入支撑乐观情景。

## 6. P6C：重整方案价值重估

### 6.1 估值对象

回答：

> 正式方案中的偿债、注资、资产变化和股本调整实施后，原股东每股权益可能怎样变化？

### 6.2 价值桥

`重整前权益价值`

`+ 债务削减和新投入现金`

`+ 已识别且有审计/评估依据的注入资产`

`- 剥离资产、剩余债务和或有负债`

`= 重整后权益价值`

再除以实施后的 fully diluted shares，形成每股区间。必须区分：

- 当前股本；
- 资本公积转增；
- 投资人受让股份；
- 债权人抵债股份；
- 原股东实际保留比例。

### 6.3 博弈条件

在成功和失败情景都可定义时，计算市场隐含的盈亏平衡成功概率：

`p* = (当前市值 - 失败情景价值) / (成功情景价值 - 失败情景价值)`

系统不把 `p*` 解释为真实成功概率。只有历史阶段转移概率经过代表性样本和概率校准后，才允许并列显示历史参照；否则只做情景敏感性。

## 7. 不依赖估值专家的人审机制

用户不审核“数字是否专业”，而是由下列机器可验证机制承担质量门。

### 7.1 事实一致性门

- 资产负债表满足会计恒等关系或解释差异。
- 同一报告期的原始披露、修订和重述不混用。
- 每个输入有 `report_period`、`announced_at`、`available_as_of`、`source_ref` 和单位。
- 历史估值只能看到估值日当时已公开的版本。
- 股本变化和复权链能从公告或权威数据重建。

### 7.2 多方法交叉验证

市场法、资产法和收益法独立运行。分歧本身是结果，不由 LLM 自动“调和”。监管和专业估值标准都强调方法适用性、输入可靠性及不同方法结果的解释，而不是选择最高或最低数字。

### 7.3 朴素基线挑战

P6B 模型至少与以下基线比较：

1. 估值日当前总市值；
2. 个股过去三年相对 ST/中证2000调整后的市值中位区间；
3. point-in-time 匹配案例市值分位区间；
4. 未调整账面净资产。

模型在时序外样本不能稳定优于基线时，不晋级为正式估值，只保留研究实验结果。

### 7.4 长期价格校准

长期价格是重要结果参考，但不是唯一真值：

- 使用总回报和 fully diluted market cap，不使用未经处理的单股价格；
- 同时扣除 ST、中证2000和中证全指的市场变化；
- 不用某一个任意终点日，使用冻结窗口内的中位市值和区间；
- 把增发、转增、退市、停牌和资产注入视为结构事件。

P6B 的校准在最早发生下列事件时截断：

- 正式重整方案关键条款披露；
- 重大资产注入/剥离；
- 退市或程序终结；
- 预先冻结的观察期结束。

如果结构事件发生，后续价格不再作为 P6B“原有价值”的标签，转入 P6C 或失败情景验证。P6C 则使用方案实施后的 6/12/24 个月市值、现金流和方案兑现事实校准。

### 7.5 时序外样本与区间评分

- 采用 rolling-origin / walk-forward；训练年份必须早于验证年份。
- 参数在 holdout 前冻结，不因看到结果回调。
- 包含失败和退市案例，禁止只回测存活股票。
- 估值区间用覆盖率和宽度共同评分；不能靠无限放宽区间获得高覆盖。
- 记录 nominal coverage、实际 coverage、median width、interval score、下界跌破率和分市场状态表现。
- 可使用 time-series conformal calibration 调整区间，但不能掩盖底层模型在新市场状态下失效。

### 7.6 晋级、降级与拒答

正式门槛在 pilot 后冻结，至少要求：

- 无 point-in-time 泄漏；
- 区间覆盖接近声明目标；
- proper interval score 优于预注册朴素基线；
- 在 ST 强弱、中小盘强弱和急跌窗口没有灾难性失真；
- 输入覆盖和或有负债达到最低证据等级。

模型上线后持续记录真实结果。校准显著恶化时自动降级为 `experimental`，AnswerCard 只展示组件、历史价格位置和缺口。

## 8. 数据面

建议新增两个 append-only 数据面：

### 8.1 `restructuring_entities_v1`

- 案件、主体、法院、管理人组织、别名、任职和关键节点；
- 原始材料内容寻址；
- 每个事实有有效期和来源等级。

### 8.2 `valuation_facts_v1`

- point-in-time 财务报表及重述链；
- 资产处置、评估、担保、诉讼、资金占用和债权事实；
- 方案股本、投资价格、债务调整、注入/剥离资产；
- 模型输入、版本、假设、输出区间和 calibration report。

不得把估值假设写回原始事实表，也不得把这两个数据面塞进 `market_factors_v1.sqlite3`。

## 9. AnswerCard 与界面

先复用 AnswerCard v0 的稳定 `body_rows`，建议新增行类型：

- `管理人任职事实`
- `管理人节点案例`
- `管理人节点分布`
- `重整前估值方法`
- `重整前估值区间`
- `估值模型分歧`
- `方案价值桥`
- `稀释后每股区间`
- `市场隐含条件`
- `估值证据缺口`
- `估值校准状态`

只有强类型消费者确实需要顶层随机访问时，才发布新 contract。

## 10. 实施顺序

1. **P6A pilot**：20–30 个上市公司重整案件，完成身份规范、联合/更换管理人和节点价格路径。
2. **P6B shadow baseline**：先做 current market cap、历史相对市值、匹配案例和账面净资产四条基线，不急于发布综合估值。
3. **P6B asset pilot**：选择 5–10 个资产结构不同的案例，材料化 point-in-time 财务和风险项，运行资产法/收益法适用性门。
4. **P6B calibration**：扩展到代表性历史样本，冻结外样本门；赢不了基线则停在实验层。
5. **P6C plan pilot**：选择已有完整正式方案、股本实施和后续价格的已完成案例，验证价值桥。
6. **Answer/UI integration**：只接入通过晋级门的行类型和模型。

P6A、P6B、P6C 分别提交，不在一个 PR 中混做。

## 11. 主要风险

- **长期价格不是内在价值真值**：使用多重标签、结构事件截断和市场相对校准。
- **幸存者偏差**：必须保留失败、退市和无方案案例。
- **旧 episode 选择偏差**：270 条旧 episode 只作启动索引，不能直接训练生产概率。
- **隐性负债缺失**：证据不足时 fail closed，不把缺失当作零。
- **模型分歧**：不机械平均，输出无共识。
- **区间过宽**：用 proper interval score 同时惩罚漏覆盖和过宽。
- **小样本管理人归因**：只做条件分布，不做因果或声誉排名。
- **方案信息污染 P6B**：以结构事件截断并转入 P6C。

## 12. 外部方法参照

- [中国证监会资产评估监管要求](https://www.csrc.gov.cn/csrc/c101802/c7118508/content.shtml)：方法适用性、多方法差异解释、可比公司/案例校验。
- [International Valuation Standards](https://www.ivsc.org/wp-content/uploads/2021/10/IVS105ValuationApproaches.pdf)：市场法、收益法和成本法；多方法结果需要合理 reconciliation。
- [Campbell、Hilscher、Szilagyi](https://www.nber.org/papers/w12362)：困境风险与杠杆、盈利、现金、市值和波动等特征相关，困境股高波动且长期结果并不天然优越。
- [Gneiting、Balabdaoui、Raftery](https://academic.oup.com/jrsssb/article/69/2/243/7109375)：概率区间应同时考察 calibration 与 sharpness，可用 proper scoring rules 评价。
- [Time-series conformal prediction](https://arxiv.org/abs/2010.09107)：用于非交换、时序数据的区间覆盖校准，但不替代正确的 point-in-time 数据和外样本设计。
