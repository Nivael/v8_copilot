# P8C-v3.1：只修价格窗口的停牌语义

2026-09-07，在任何本轮收益结果读取之前冻结。承接 v3；旧契约、旧结果保留。
本次授权是修复过严的数据筛选，不是寻找更有利的量价参数。

## 不变项

`p8_grid_config.json` 仍为唯一配置。六个经济阈值、全部时间窗、20日冷却、40段/25家公司
共同层样本门、两项主检验、阶段/半年/公告/股本分层、收益端点及双退市终值均不变。
20日活动窗的合格性门本轮不改；不同时修改多处筛选再把增量归给停牌修复。
历史已被阅读，本轮仍是探索，生产晋级配额仍为0。

## 价格窗口修正

仍是截至 t 的最近250个**市场交易日**，不扩展为250个股票实际交易日。
仅在有 `suspend_d` 全天停牌正证据时，从价格分位的分母中排除该日；不填旧价格、不插值。
全天条件：类型严格为 S，日内停牌时段为空；R（复牌）或有时段的临停不能通过。
若本地原始收盘价存在，与全天停牌证据冲突，不能用该证据豁免价格缺失。
依据：[Tushare 每日停复牌文档](https://tushare.pro/document/2?doc_id=214)。

当日无真实 qfq 收盘不生成网格；窗口不足250个市场交易日仍为历史不足。
其他任一缺失/无效价格仍为 unknown，包括入ST前没有活动记录的日子。
窗口内实际价格观察数、排除的已证停牌日期和窗口起点随每个网格/episode保存；
股本标记仍覆盖相同的250个市场交易日，不因价格缺失缩短。

## 无收益数据计划

1. 只读既有 qfq、历史 membership、活动事实，枚举会被网格读取的窗口内缺价股票日。
2. 已有全天停牌证据直接复用；仅对尚无正证据的缺口按唯一日期请求 `suspend_d`。
   请求域限于已声明历史ST成员的历史窗口，不拉公告正文、不改任何生产数据库。
3. 计划记录目标股票日、唯一日期、请求上限及输入摘要；逐日成功缓存，失败保持 pending。
   空响应不构成停牌证据。保留原始返回、抓取日期和摘要；复牌/临停/冲突均不豁免。
4. 先完成同代码/配置/证据的无结果库存，冻结摘要后才运行一次收益评估。
   每年输出 membership→价格门→活动门→低位价稳活跃股票日→去重episode 的筛选账。

停牌核证层可独立存在于本地派生目录；没有覆盖的缺口继续列示，不为凑18段而跳过。
不新增人审任务。真实价格缺失若需额外回填，先输出精确数据债，禁止以停牌代替。
