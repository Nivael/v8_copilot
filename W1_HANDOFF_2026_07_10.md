# W1 Core / Contract handoff

日期：2026-07-10
状态：W1 P1 core gate passed
契约：`v8_answer_contract_v0`

## 已完成

- 固定 Python 3.12 + `uv` 项目环境，生成 `uv.lock`；
- 发布 `contracts/v8_answer_contract_v0/schema.json`；
- `AnswerCard` 增加 `contract_version`、稳定 `row_id`、`AnalysisClaim.backing`；
- Python validator 校验 lens spine、gap sediment、data debt refs、claim backing、evidence 身份、source freshness、固定 caveat 和禁用措辞；
- LensRegistry 对缺文件、坏 JSON、重复 id、未知 role、不完整 evidence record fail loudly；
- LensInvocation 完整保留逻辑链、允许/禁止措辞和全部 provenance refs；
- seed cards 从 3 张扩到 7 张，覆盖 query、checklist、evidence、data debt 和 lens gap；
- RL-A-001/RL-A-002 evidence-first 卡展示 N、effect digest、counterexample 和 wording boundary；
- 空 episode subtype 稳定返回 N=0，不再崩溃；
- 单票 ST 生命周期保留不同 source 的重叠行，不擅自清洗冲突。

## 验证

```bash
uv sync --group dev
uv lock --check
uv run pytest
uv run python run_seeds.py
uv run python -B evals/validate_w2_evals.py
```

结果：

- W1 tests：19 passed；
- seed cards：7/7 通过 Python contract；
- 7/7 通过 JSON Schema；
- W2 30 题路由资产兼容性检查通过；
- 三条旧切片基线保持：4/10/14 天；两周 p05/p50/p95 = -15.5%/-0.5%/17.6%；沐邦 checklist 保持。

## 跨窗口边界

- W2/W3/LLM 只读 `v8_answer_contract_v0`；缺字段时提交缺口，不直接改 schema。
- W2 继续拥有 `evals/`；W1 未修改 W2 文件。
- W3 继续拥有 `ui/` 和 `DESIGN.md`；W1 本轮未修改 UI。
- 新事实字段必须先进入 deterministic Core 和 AnswerCard，再暴露给 LLM/UI。

## 尚未完成

- 当前目录尚未由 W0 固化为正式 git baseline；
- 任意问题 router、API、LLM 编排、主 Copilot 面板不属于本轮 W1；
- lens candidate matching 仍是保守的 cluster/topic-label 规则；
- ST 生命周期不同 source 的重叠尚未裁决，只显式保留并加 caveat。
